--select * from dbo.bands_band;
-- Drop a table called 'TableName' in schema 'dbo'
-- Drop the table if it already exists
--IF OBJECT_ID('[dbo].[TableName]', 'U') IS NOT NULL
--DROP TABLE [dbo].[TableName]
--GO

--IF OBJECT_ID('dbo.bands_band', 'U') IS NOT NULL
--DROP TABLE dbo.bands_band
--GO

-- Create a new table called '[TableName]' in schema '[dbo]'
-- Drop the table if it already exists
--IF OBJECT_ID('[dbo].[TableName]', 'U') IS NOT NULL
--DROP TABLE [dbo].[TableName]
--GO
-- Create the table in the specified schema
-- CREATE TABLE [dbo].[TableName]
-- (
--     [Id] INT NOT NULL PRIMARY KEY, -- Primary Key column
--     [ColumnName2] NVARCHAR(50) NOT NULL,
--     [ColumnName3] NVARCHAR(50) NOT NULL
--     -- Specify more columns here
-- );
-- GO


